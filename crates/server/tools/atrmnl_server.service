[Unit]
Description="Awesome TRMNL Server"
After=network.target

[Service]
Type=exec
WorkingDirectory=~
ExecStart=%h/.cargo/bin/atrmnl_server -p 8223 -d %h/.config/atrmnl_server/devices.toml
Restart=on-failure

[Install]
WantedBy=default.target